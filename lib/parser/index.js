/* parser generated by jison 0.4.15 */
/*
  Returns a Parser object of the following structure:

  Parser: {
    yy: {}
  }

  Parser.prototype: {
    yy: {},
    trace: function(),
    symbols_: {associative list: name ==> number},
    terminals_: {associative list: number ==> name},
    productions_: [...],
    performAction: function anonymous(yytext, yyleng, yylineno, yy, yystate, $$, _$),
    table: [...],
    defaultActions: {...},
    parseError: function(str, hash),
    parse: function(input),

    lexer: {
        EOF: 1,
        parseError: function(str, hash),
        setInput: function(input),
        input: function(),
        unput: function(str),
        more: function(),
        less: function(n),
        pastInput: function(),
        upcomingInput: function(),
        showPosition: function(),
        test_match: function(regex_match_array, rule_index),
        next: function(),
        lex: function(),
        begin: function(condition),
        popState: function(),
        _currentRules: function(),
        topState: function(),
        pushState: function(condition),

        options: {
            ranges: boolean           (optional: true ==> token location info will include a .range[] member)
            flex: boolean             (optional: true ==> flex-like lexing behaviour where the rules are tested exhaustively to find the longest match)
            backtrack_lexer: boolean  (optional: true ==> lexer regexes are tested in order and for each matching regex the action code is invoked; the lexer terminates the scan when a token is returned by the action code)
        },

        performAction: function(yy, yy_, $avoiding_name_collisions, YY_START),
        rules: [...],
        conditions: {associative list: name ==> set},
    }
  }


  token location info (@$, _$, etc.): {
    first_line: n,
    last_line: n,
    first_column: n,
    last_column: n,
    range: [start_number, end_number]       (where the numbers are indexes into the input string, regular zero-based)
  }


  the parseError function receives a 'hash' object with these members for lexer and parser errors: {
    text:        (matched text)
    token:       (the produced terminal token, if any)
    line:        (yylineno)
  }
  while parser (grammar) errors will also provide these members, i.e. parser errors deliver a superset of attributes: {
    loc:         (yylloc)
    expected:    (string describing the set of expected tokens)
    recoverable: (boolean: TRUE when the parser has a error recovery rule available for this particular error)
  }
*/
var parser = (function(){
var o=function(k,v,o,l){for(o=o||{},l=k.length;l--;o[k[l]]=v);return o},$V0=[1,12],$V1=[1,13],$V2=[1,10],$V3=[1,11],$V4=[1,14],$V5=[1,15],$V6=[1,16],$V7=[1,17],$V8=[1,18],$V9=[1,19],$Va=[1,20],$Vb=[4,12,13,14,16,17,24,29,30,45,46,57],$Vc=[24,67,68],$Vd=[29,70,71,72],$Ve=[45,49],$Vf=[60,74],$Vg=[29,71,72],$Vh=[22,24],$Vi=[65,75],$Vj=[29,37,73],$Vk=[45,52],$Vl=[4,12,13,14,16,17,24,29,30,45,46,57,69],$Vm=[37,73],$Vn=[4,12,13,14,16,17,24,29,30,45,46,57,74],$Vo=[62,65],$Vp=[62,65,75];
var parser = {trace: function trace() { },
yy: {},
symbols_: {"error":2,"root":3,"EOF":4,"rules":5,"mulit_comment":6,"single_comment":7,"charset_stmt":8,"import_stmt":9,"variable_stmt":10,"selector_stmt":11,"SC":12,"S_SPACE":13,"MC":14,"MC_END":15,"M_SPACE":16,"CH_START":17,"CHARSET":18,"charset_stmt_repetition0":19,"charset_stmt_group0":20,"charset_stmt_repetition1":21,"CH_SEMICOLON":22,"charset_stmt_repetition2":23,"CH_SPACE":24,"charset_stmt_repetition3":25,"charset_stmt_group1":26,"charset_stmt_repetition4":27,"charset_stmt_repetition5":28,"IM_SPACE":29,"IM_START":30,"IMPORT":31,"import_stmt_repetition0":32,"import_stmt_repetition1":33,"import_stmt_group0":34,"import_stmt_repetition2":35,"import_stmt_repetition3":36,"IM_SEMICOLON":37,"import_stmt_repetition4":38,"import_stmt_repetition5":39,"import_stmt_repetition6":40,"import_stmt_group1":41,"import_stmt_repetition7":42,"import_stmt_repetition8":43,"import_stmt_repetition9":44,"VARI_SPACE":45,"VARI_START":46,"VARI_NAME":47,"variable_stmt_repetition0":48,"VARI_COLON":49,"VARI_VALUE":50,"variable_stmt_repetition1":51,"VARI_SEMICOLON":52,"variable_stmt_repetition2":53,"variable_stmt_repetition3":54,"variable_stmt_repetition4":55,"variable_stmt_repetition5":56,"SEL_SPACE_BEFORE_SELECTOR":57,"SEL_SELECTOR":58,"selector_stmt_repetition0":59,"LEFT_BRACE":60,"block_stmt":61,"RIGHT_BRACE":62,"selector_stmt_repetition1":63,"block_stmt_repetition0":64,"BLOCK_SEMICOLON":65,"block_stmt_repetition1":66,"CH_STRING":67,"CH_LETTER":68,"SPACE":69,"IM_OPT":70,"IM_STRING":71,"IM_URL":72,"IM_MEDIA":73,"SEL_SPACE":74,"BLOCK_SPACE":75,"$accept":0,"$end":1},
terminals_: {2:"error",4:"EOF",12:"SC",13:"S_SPACE",14:"MC",15:"MC_END",16:"M_SPACE",17:"CH_START",18:"CHARSET",22:"CH_SEMICOLON",24:"CH_SPACE",29:"IM_SPACE",30:"IM_START",31:"IMPORT",37:"IM_SEMICOLON",45:"VARI_SPACE",46:"VARI_START",47:"VARI_NAME",49:"VARI_COLON",50:"VARI_VALUE",52:"VARI_SEMICOLON",57:"SEL_SPACE_BEFORE_SELECTOR",58:"SEL_SELECTOR",60:"LEFT_BRACE",62:"RIGHT_BRACE",65:"BLOCK_SEMICOLON",67:"CH_STRING",68:"CH_LETTER",69:"SPACE",70:"IM_OPT",71:"IM_STRING",72:"IM_URL",73:"IM_MEDIA",74:"SEL_SPACE",75:"BLOCK_SPACE"},
productions_: [0,[3,1],[3,2],[5,1],[5,2],[5,1],[5,2],[5,1],[5,2],[5,1],[5,2],[5,1],[5,2],[5,1],[5,2],[7,1],[7,2],[6,2],[6,3],[8,7],[8,8],[9,10],[9,9],[10,9],[10,8],[11,7],[61,3],[61,2],[19,0],[19,2],[20,1],[20,1],[21,0],[21,2],[23,0],[23,2],[25,0],[25,2],[26,1],[26,1],[27,0],[27,2],[28,0],[28,2],[32,0],[32,2],[33,0],[33,2],[34,1],[34,1],[35,0],[35,2],[36,0],[36,2],[38,0],[38,2],[39,0],[39,2],[40,0],[40,2],[41,1],[41,1],[42,0],[42,2],[43,0],[43,2],[44,0],[44,2],[48,0],[48,2],[51,0],[51,2],[53,0],[53,2],[54,0],[54,2],[55,0],[55,2],[56,0],[56,2],[59,0],[59,2],[63,0],[63,2],[64,0],[64,2],[66,0],[66,2]],
performAction: function anonymous(yytext, yyleng, yylineno, yy, yystate /* action[1] */, $$ /* vstack */, _$ /* lstack */) {
/* this == yyval */

var $0 = $$.length - 1;
switch (yystate) {
case 1: case 2:

        return {
            root: ast
        };
    
break;
case 15:

        ast.sComments.push({
            type: 'sComment',
            originContent: $$[$0],
            value: $$[$0],
            before: '',
            after: '',
            loc: {
                firstLine: _$[$0].first_line,
                lastLine: _$[$0].last_line,
                firstCol: _$[$0].first_column + 1,
                lastCol: _$[$0].last_column + 1
            }
        });
    
break;
case 16:

        ast.sComments.push({
            type: 'sComment',
            originContent: $$[$0],
            value: $$[$0],
            before: $$[$0-1],
            after: '',
            loc: {
                firstLine: _$[$0].first_line,
                lastLine: _$[$0].last_line,
                firstCol: _$[$0].first_column + 1,
                lastCol: _$[$0].last_column + 1
            }
        });
    
break;
case 17:

        // yy.test();
        ast.mComments.push({
            type: 'mComment',
            value: $$[$0-1] + $$[$0],
            before: '',
            after: '',
            loc: {
                firstLine: _$[$0-1].first_line,
                lastLine: _$[$0].last_line,
                firstCol: _$[$0-1].first_column + 1,
                lastCol: _$[$0].last_column + 1,
                originContent: $$[$0-1] + $$[$0]
            }
        });
    
break;
case 18:

        ast.mComments.push({
            type: 'mComment',
            value: $$[$0-1] + $$[$0],
            before: $$[$0-2],
            after: '',
            loc: {
                firstLine: _$[$0-1].first_line,
                lastLine: _$[$0].last_line,
                firstCol: _$[$0-1].first_column + 1,
                lastCol: _$[$0].last_column + 1,
                originContent: $$[$0-1] + $$[$0]
            }
        });
    
break;
case 19:

        var quote = '';
        var match;
        if (match = $$[$0-3].match(/^(['"]).*\1/)) {
            quote = match[1];
        }
        ast.charsets.push({
            type: 'charset',
            originContent: $$[$0-6] + $$[$0-5] + $$[$0-4] + $$[$0-3] + $$[$0-2] + $$[$0-1] + $$[$0],
            value: $$[$0-4].join('') + $$[$0-3] + $$[$0-2].join(''),
            quote: quote,
            before: '',
            after: '',
            loc: {
                firstLine: _$[$0-6].first_line,
                lastLine: _$[$0-1].last_line,
                firstCol: _$[$0-6].first_column + 1,
                lastCol: _$[$0-1].last_column + 1
            }
        });
    
break;
case 20:

        var quote = '';
        var match;
        if (match = $$[$0-3].match(/^(['"]).*\1/)) {
            quote = match[1];
        }
        ast.charsets.push({
            type: 'charset',
            originContent: $$[$0-7] + $$[$0-6] + $$[$0-5] + $$[$0-4] + $$[$0-3] + $$[$0-2] + $$[$0-1] + $$[$0],
            value: $$[$0-4].join('') + $$[$0-3] + $$[$0-2].join(''),
            quote: quote,
            before: $$[$0-7],
            after: '',
            loc: {
                firstLine: _$[$0-6].first_line,
                lastLine: _$[$0-1].last_line,
                firstCol: _$[$0-6].first_column + 1,
                lastCol: _$[$0-1].last_column + 1
            }
        });
    
break;
case 21:

        var quote = '';
        var match;
        if (match = $$[$0-4].match(/(['"]).*\1/)) {
            quote = match[1];
        }

        var importOption = [];
        var imOptStr = $$[$0-6].join('');
        if (imOptStr) {
            var t = imOptStr.split(',');
            var s;
            for (var i = 0, len = t.length; i < len; i++) {
                s = t[i].replace(/^[\s\(]*/g, '').replace(/[\s\)]*$/, '');
                if (s) {
                    importOption.push(s);
                }
            }
        }

        ast.imports.push({
            type: 'import',
            originContent: $$[$0-9] + $$[$0-8] + $$[$0-7] + $$[$0-6] + $$[$0-5] + $$[$0-4] + $$[$0-3] + $$[$0-2] + $$[$0-1] + $$[$0],
            value: $$[$0-5].join('') + $$[$0-4] + $$[$0-3].join(''),
            quote: quote,
            importOption: importOption,
            originImportOption: imOptStr,
            mediaValue: $$[$0-2].join(''),
            before: $$[$0-9],
            after: $$[$0].join(''),
            loc: {
                firstLine: _$[$0-8].first_line,
                lastLine: _$[$0-1].last_line,
                firstCol: _$[$0-8].first_column + 1,
                lastCol: _$[$0-1].last_column + 1
            }
        });
    
break;
case 22:

        var quote = '';
        var match;
        if (match = $$[$0-4].match(/(['"]).*\1/)) {
            quote = match[1];
        }

        var importOption = [];
        var imOptStr = $$[$0-6].join('');
        if (imOptStr) {
            var t = imOptStr.split(',');
            var s;
            for (var i = 0, len = t.length; i < len; i++) {
                s = t[i].replace(/^[\s\(]*/g, '').replace(/[\s\)]*$/, '');
                if (s) {
                    importOption.push(s);
                }
            }
        }

        ast.imports.push({
            type: 'import',
            originContent: $$[$0-8] + $$[$0-7] + $$[$0-6] + $$[$0-5] + $$[$0-4] + $$[$0-3] + $$[$0-2] + $$[$0-1] + $$[$0],
            value: $$[$0-5].join('') + $$[$0-4] + $$[$0-3].join(''),
            quote: quote,
            importOption: importOption,
            originImportOption: imOptStr,
            mediaValue: $$[$0-2].join(''),
            before: '',
            after: $$[$0].join(''),
            loc: {
                firstLine: _$[$0-8].first_line,
                lastLine: _$[$0-1].last_line,
                firstCol: _$[$0-8].first_column + 1,
                lastCol: _$[$0-1].last_column + 1
            }
        });
    
break;
case 23:

        var valueBefore = '';
        var match = /^(\s+)/.exec($$[$0-3]);
        if (match) {
            valueBefore = match[0];
        }

        var pureValue = $$[$0-3].replace(/^(\s+)/, '');

        ast.variables.push({
            type: 'variable',
            originContent: $$[$0-8] + $$[$0-7] + $$[$0-6] + $$[$0-5] + $$[$0-4] + $$[$0-3] + $$[$0-2] + $$[$0-1] + $$[$0],
            variableName: $$[$0-6],
            variableNameBefore: $$[$0-8],
            variablenameAfter: $$[$0-5].join(''),
            variableValue: pureValue,
            variableValueBefore: valueBefore,
            variableValueAfter: $$[$0-2].join(''),
            value: pureValue,
            before: $$[$0-8],
            after: $$[$0].join(''),
            loc: {
                firstLine: _$[$0-7].first_line,
                lastLine: _$[$0-1].last_line,
                firstCol: _$[$0-7].first_column + 1,
                lastCol: _$[$0-1].last_column + 1
            }
        });
    
break;
case 24:

        var valueBefore = '';
        var match = /^(\s+)/.exec($$[$0-3]);
        if (match) {
            valueBefore = match[0];
        }

        var pureValue = $$[$0-3].replace(/^(\s+)/, '');

        ast.variables.push({
            type: 'variable',
            originContent: $$[$0-7] + $$[$0-6] + $$[$0-5] + $$[$0-4] + $$[$0-3] + $$[$0-2] + $$[$0-1] + $$[$0],
            variableName: $$[$0-6],
            variableNameBefore: '',
            variablenameAfter: $$[$0-5].join(''),
            variableValue: pureValue,
            variableValueBefore: valueBefore,
            variableValueAfter: $$[$0-2].join(''),
            value: pureValue,
            before: '',
            after: $$[$0].join(''),
            loc: {
                firstLine: _$[$0-7].first_line,
                lastLine: _$[$0-1].last_line,
                firstCol: _$[$0-7].first_column + 1,
                lastCol: _$[$0-1].last_column + 1
            }
        });
    
break;
case 25:

        console.warn($$[$0-6], $$[$0-5]);
        console.warn(123123);
    
break;
case 26:

        console.warn($$[$0-2], 'sssss');
    
break;
case 27:


    
break;
case 28: case 32: case 34: case 36: case 40: case 42: case 44: case 46: case 50: case 52: case 54: case 56: case 58: case 62: case 64: case 66: case 68: case 70: case 72: case 74: case 76: case 78: case 80: case 82: case 84: case 86:
this.$ = [];
break;
case 29: case 33: case 35: case 37: case 41: case 43: case 45: case 47: case 51: case 53: case 55: case 57: case 59: case 63: case 65: case 67: case 69: case 71: case 73: case 75: case 77: case 79: case 81: case 83: case 85: case 87:
$$[$0-1].push($$[$0]);
break;
}
},
table: [{3:1,4:[1,2],5:3,6:4,7:5,8:6,9:7,10:8,11:9,12:$V0,13:$V1,14:$V2,16:$V3,17:$V4,24:$V5,29:$V6,30:$V7,45:$V8,46:$V9,57:$Va},{1:[3]},{1:[2,1]},{4:[1,21],6:22,7:23,8:24,9:25,10:26,11:27,12:$V0,13:$V1,14:$V2,16:$V3,17:$V4,24:$V5,29:$V6,30:$V7,45:$V8,46:$V9,57:$Va},o($Vb,[2,3]),o($Vb,[2,5]),o($Vb,[2,7]),o($Vb,[2,9]),o($Vb,[2,11]),o($Vb,[2,13]),{15:[1,28]},{14:[1,29]},o($Vb,[2,15]),{12:[1,30]},{18:[1,31]},{17:[1,32]},{30:[1,33]},{31:[1,34]},{46:[1,35]},{47:[1,36]},{58:[1,37]},{1:[2,2]},o($Vb,[2,4]),o($Vb,[2,6]),o($Vb,[2,8]),o($Vb,[2,10]),o($Vb,[2,12]),o($Vb,[2,14]),o($Vb,[2,17]),{15:[1,38]},o($Vb,[2,16]),o($Vc,[2,28],{19:39}),{18:[1,40]},{31:[1,41]},o($Vd,[2,56],{39:42}),{47:[1,43]},o($Ve,[2,74],{54:44}),o($Vf,[2,80],{59:45}),o($Vb,[2,18]),{20:46,24:[1,47],67:[1,48],68:[1,49]},o($Vc,[2,36],{25:50}),o($Vd,[2,44],{32:51}),o($Vg,[2,58],{40:52,70:[1,53]}),o($Ve,[2,68],{48:54}),{45:[1,56],49:[1,55]},{60:[1,57],74:[1,58]},o($Vh,[2,32],{21:59}),o($Vc,[2,29]),o($Vh,[2,30]),o($Vh,[2,31]),{24:[1,61],26:60,67:[1,62],68:[1,63]},o($Vg,[2,46],{33:64,70:[1,65]}),{29:[1,67],41:66,71:[1,68],72:[1,69]},o($Vd,[2,57]),{45:[1,71],49:[1,70]},{50:[1,72]},o($Ve,[2,75]),o($Vi,[2,84],{61:73,64:74}),o($Vf,[2,81]),{22:[1,75],24:[1,76]},o($Vh,[2,40],{27:77}),o($Vc,[2,37]),o($Vh,[2,38]),o($Vh,[2,39]),{29:[1,79],34:78,71:[1,80],72:[1,81]},o($Vd,[2,45]),o($Vj,[2,62],{42:82}),o($Vg,[2,59]),o($Vj,[2,60]),o($Vj,[2,61]),{50:[1,83]},o($Ve,[2,69]),o($Vk,[2,76],{55:84}),{62:[1,85],65:[1,86]},{65:[1,87],75:[1,88]},o($Vl,[2,34],{23:89}),o($Vh,[2,33]),{22:[1,90],24:[1,91]},o($Vj,[2,50],{35:92}),o($Vg,[2,47]),o($Vj,[2,48]),o($Vj,[2,49]),o($Vm,[2,64],{43:93,29:[1,94]}),o($Vk,[2,70],{51:95}),{45:[1,97],52:[1,96]},o($Vn,[2,82],{63:98}),o($Vo,[2,27]),o($Vp,[2,86],{66:99}),o($Vi,[2,85]),o($Vb,[2,19],{69:[1,100]}),o($Vl,[2,42],{28:101}),o($Vh,[2,41]),o($Vm,[2,52],{36:102,29:[1,103]}),{37:[1,104],73:[1,105]},o($Vj,[2,63]),{45:[1,107],52:[1,106]},o($Vl,[2,78],{56:108}),o($Vk,[2,77]),o($Vb,[2,25],{74:[1,109]}),o($Vo,[2,26],{75:[1,110]}),o($Vl,[2,35]),o($Vb,[2,20],{69:[1,111]}),{37:[1,112],73:[1,113]},o($Vj,[2,51]),o($Vl,[2,66],{44:114}),o($Vm,[2,65]),o($Vl,[2,72],{53:115}),o($Vk,[2,71]),o($Vb,[2,24],{69:[1,116]}),o($Vn,[2,83]),o($Vp,[2,87]),o($Vl,[2,43]),o($Vl,[2,54],{38:117}),o($Vm,[2,53]),o($Vb,[2,22],{69:[1,118]}),o($Vb,[2,23],{69:[1,119]}),o($Vl,[2,79]),o($Vb,[2,21],{69:[1,120]}),o($Vl,[2,67]),o($Vl,[2,73]),o($Vl,[2,55])],
defaultActions: {2:[2,1],21:[2,2]},
parseError: function parseError(str, hash) {
    if (hash.recoverable) {
        this.trace(str);
    } else {
        throw new Error(str);
    }
},
parse: function parse(input) {
    var self = this, stack = [0], tstack = [], vstack = [null], lstack = [], table = this.table, yytext = '', yylineno = 0, yyleng = 0, recovering = 0, TERROR = 2, EOF = 1;
    var args = lstack.slice.call(arguments, 1);
    var lexer = Object.create(this.lexer);
    var sharedState = { yy: {} };
    for (var k in this.yy) {
        if (Object.prototype.hasOwnProperty.call(this.yy, k)) {
            sharedState.yy[k] = this.yy[k];
        }
    }
    lexer.setInput(input, sharedState.yy);
    sharedState.yy.lexer = lexer;
    sharedState.yy.parser = this;
    if (typeof lexer.yylloc == 'undefined') {
        lexer.yylloc = {};
    }
    var yyloc = lexer.yylloc;
    lstack.push(yyloc);
    var ranges = lexer.options && lexer.options.ranges;
    if (typeof sharedState.yy.parseError === 'function') {
        this.parseError = sharedState.yy.parseError;
    } else {
        this.parseError = Object.getPrototypeOf(this).parseError;
    }
    function popStack(n) {
        stack.length = stack.length - 2 * n;
        vstack.length = vstack.length - n;
        lstack.length = lstack.length - n;
    }
    _token_stack:
        function lex() {
            var token;
            token = lexer.lex() || EOF;
            if (typeof token !== 'number') {
                token = self.symbols_[token] || token;
            }
            return token;
        }
    var symbol, preErrorSymbol, state, action, a, r, yyval = {}, p, len, newState, expected;
    while (true) {
        state = stack[stack.length - 1];
        if (this.defaultActions[state]) {
            action = this.defaultActions[state];
        } else {
            if (symbol === null || typeof symbol == 'undefined') {
                symbol = lex();
            }
            action = table[state] && table[state][symbol];
        }
                    if (typeof action === 'undefined' || !action.length || !action[0]) {
                var errStr = '';
                expected = [];
                for (p in table[state]) {
                    if (this.terminals_[p] && p > TERROR) {
                        expected.push('\'' + this.terminals_[p] + '\'');
                    }
                }
                if (lexer.showPosition) {
                    errStr = 'Parse error on line ' + (yylineno + 1) + ':\n' + lexer.showPosition() + '\nExpecting ' + expected.join(', ') + ', got \'' + (this.terminals_[symbol] || symbol) + '\'';
                } else {
                    errStr = 'Parse error on line ' + (yylineno + 1) + ': Unexpected ' + (symbol == EOF ? 'end of input' : '\'' + (this.terminals_[symbol] || symbol) + '\'');
                }
                this.parseError(errStr, {
                    text: lexer.match,
                    token: this.terminals_[symbol] || symbol,
                    line: lexer.yylineno,
                    loc: yyloc,
                    expected: expected
                });
            }
        if (action[0] instanceof Array && action.length > 1) {
            throw new Error('Parse Error: multiple actions possible at state: ' + state + ', token: ' + symbol);
        }
        switch (action[0]) {
        case 1:
            stack.push(symbol);
            vstack.push(lexer.yytext);
            lstack.push(lexer.yylloc);
            stack.push(action[1]);
            symbol = null;
            if (!preErrorSymbol) {
                yyleng = lexer.yyleng;
                yytext = lexer.yytext;
                yylineno = lexer.yylineno;
                yyloc = lexer.yylloc;
                if (recovering > 0) {
                    recovering--;
                }
            } else {
                symbol = preErrorSymbol;
                preErrorSymbol = null;
            }
            break;
        case 2:
            len = this.productions_[action[1]][1];
            yyval.$ = vstack[vstack.length - len];
            yyval._$ = {
                first_line: lstack[lstack.length - (len || 1)].first_line,
                last_line: lstack[lstack.length - 1].last_line,
                first_column: lstack[lstack.length - (len || 1)].first_column,
                last_column: lstack[lstack.length - 1].last_column
            };
            if (ranges) {
                yyval._$.range = [
                    lstack[lstack.length - (len || 1)].range[0],
                    lstack[lstack.length - 1].range[1]
                ];
            }
            r = this.performAction.apply(yyval, [
                yytext,
                yyleng,
                yylineno,
                sharedState.yy,
                action[1],
                vstack,
                lstack
            ].concat(args));
            if (typeof r !== 'undefined') {
                return r;
            }
            if (len) {
                stack = stack.slice(0, -1 * len * 2);
                vstack = vstack.slice(0, -1 * len);
                lstack = lstack.slice(0, -1 * len);
            }
            stack.push(this.productions_[action[1]][0]);
            vstack.push(yyval.$);
            lstack.push(yyval._$);
            newState = table[stack[stack.length - 2]][stack[stack.length - 1]];
            stack.push(newState);
            break;
        case 3:
            return true;
        }
    }
    return true;
}};


    // var chalk = require('chalk');

    // var isDebug = true;
    // function debug() {
    //     if (isDebug) {
    //         var args = [].slice.call(arguments);
    //         var len = args.length;
    //         if (len === 1) {
    //             console.warn(args[0]);
    //         }
    //         else {
    //             var msg = [];
    //             while (len) {
    //                 msg.push(args[args.length - len--]);
    //             }

    //             var first = msg.splice(0, 1);
    //             console.warn(chalk.yellow(first) + ': ' + chalk.cyan(msg.join(' ')));
    //             console.warn();
    //         }
    //     }
    // }

    var util = require('util');
    var debug = require('debug')('jison-lesslint: grammar');

    var ast = {
        variables: [],
        imports: [],
        selectors: [],
        charsets: [],
        sComments: [],
        mComments: []
    };

/* generated by jison-lex 0.3.4 */
var lexer = (function(){
var lexer = ({

EOF:1,

parseError:function parseError(str, hash) {
        if (this.yy.parser) {
            this.yy.parser.parseError(str, hash);
        } else {
            throw new Error(str);
        }
    },

// resets the lexer, sets new input
setInput:function (input, yy) {
        this.yy = yy || this.yy || {};
        this._input = input;
        this._more = this._backtrack = this.done = false;
        this.yylineno = this.yyleng = 0;
        this.yytext = this.matched = this.match = '';
        this.conditionStack = ['INITIAL'];
        this.yylloc = {
            first_line: 1,
            first_column: 0,
            last_line: 1,
            last_column: 0
        };
        if (this.options.ranges) {
            this.yylloc.range = [0,0];
        }
        this.offset = 0;
        return this;
    },

// consumes and returns one char from the input
input:function () {
        var ch = this._input[0];
        this.yytext += ch;
        this.yyleng++;
        this.offset++;
        this.match += ch;
        this.matched += ch;
        var lines = ch.match(/(?:\r\n?|\n).*/g);
        if (lines) {
            this.yylineno++;
            this.yylloc.last_line++;
        } else {
            this.yylloc.last_column++;
        }
        if (this.options.ranges) {
            this.yylloc.range[1]++;
        }

        this._input = this._input.slice(1);
        return ch;
    },

// unshifts one char (or a string) into the input
unput:function (ch) {
        var len = ch.length;
        var lines = ch.split(/(?:\r\n?|\n)/g);

        this._input = ch + this._input;
        this.yytext = this.yytext.substr(0, this.yytext.length - len);
        //this.yyleng -= len;
        this.offset -= len;
        var oldLines = this.match.split(/(?:\r\n?|\n)/g);
        this.match = this.match.substr(0, this.match.length - 1);
        this.matched = this.matched.substr(0, this.matched.length - 1);

        if (lines.length - 1) {
            this.yylineno -= lines.length - 1;
        }
        var r = this.yylloc.range;

        this.yylloc = {
            first_line: this.yylloc.first_line,
            last_line: this.yylineno + 1,
            first_column: this.yylloc.first_column,
            last_column: lines ?
                (lines.length === oldLines.length ? this.yylloc.first_column : 0)
                 + oldLines[oldLines.length - lines.length].length - lines[0].length :
              this.yylloc.first_column - len
        };

        if (this.options.ranges) {
            this.yylloc.range = [r[0], r[0] + this.yyleng - len];
        }
        this.yyleng = this.yytext.length;
        return this;
    },

// When called from action, caches matched text and appends it on next action
more:function () {
        this._more = true;
        return this;
    },

// When called from action, signals the lexer that this rule fails to match the input, so the next matching rule (regex) should be tested instead.
reject:function () {
        if (this.options.backtrack_lexer) {
            this._backtrack = true;
        } else {
            return this.parseError('Lexical error on line ' + (this.yylineno + 1) + '. You can only invoke reject() in the lexer when the lexer is of the backtracking persuasion (options.backtrack_lexer = true).\n' + this.showPosition(), {
                text: "",
                token: null,
                line: this.yylineno
            });

        }
        return this;
    },

// retain first n characters of the match
less:function (n) {
        this.unput(this.match.slice(n));
    },

// displays already matched input, i.e. for error messages
pastInput:function () {
        var past = this.matched.substr(0, this.matched.length - this.match.length);
        return (past.length > 20 ? '...':'') + past.substr(-20).replace(/\n/g, "");
    },

// displays upcoming input, i.e. for error messages
upcomingInput:function () {
        var next = this.match;
        if (next.length < 20) {
            next += this._input.substr(0, 20-next.length);
        }
        return (next.substr(0,20) + (next.length > 20 ? '...' : '')).replace(/\n/g, "");
    },

// displays the character position where the lexing error occurred, i.e. for error messages
showPosition:function () {
        var pre = this.pastInput();
        var c = new Array(pre.length + 1).join("-");
        return pre + this.upcomingInput() + "\n" + c + "^";
    },

// test the lexed token: return FALSE when not a match, otherwise return token
test_match:function (match, indexed_rule) {
        var token,
            lines,
            backup;

        if (this.options.backtrack_lexer) {
            // save context
            backup = {
                yylineno: this.yylineno,
                yylloc: {
                    first_line: this.yylloc.first_line,
                    last_line: this.last_line,
                    first_column: this.yylloc.first_column,
                    last_column: this.yylloc.last_column
                },
                yytext: this.yytext,
                match: this.match,
                matches: this.matches,
                matched: this.matched,
                yyleng: this.yyleng,
                offset: this.offset,
                _more: this._more,
                _input: this._input,
                yy: this.yy,
                conditionStack: this.conditionStack.slice(0),
                done: this.done
            };
            if (this.options.ranges) {
                backup.yylloc.range = this.yylloc.range.slice(0);
            }
        }

        lines = match[0].match(/(?:\r\n?|\n).*/g);
        if (lines) {
            this.yylineno += lines.length;
        }
        this.yylloc = {
            first_line: this.yylloc.last_line,
            last_line: this.yylineno + 1,
            first_column: this.yylloc.last_column,
            last_column: lines ?
                         lines[lines.length - 1].length - lines[lines.length - 1].match(/\r?\n?/)[0].length :
                         this.yylloc.last_column + match[0].length
        };
        this.yytext += match[0];
        this.match += match[0];
        this.matches = match;
        this.yyleng = this.yytext.length;
        if (this.options.ranges) {
            this.yylloc.range = [this.offset, this.offset += this.yyleng];
        }
        this._more = false;
        this._backtrack = false;
        this._input = this._input.slice(match[0].length);
        this.matched += match[0];
        token = this.performAction.call(this, this.yy, this, indexed_rule, this.conditionStack[this.conditionStack.length - 1]);
        if (this.done && this._input) {
            this.done = false;
        }
        if (token) {
            return token;
        } else if (this._backtrack) {
            // recover context
            for (var k in backup) {
                this[k] = backup[k];
            }
            return false; // rule action called reject() implying the next rule should be tested instead.
        }
        return false;
    },

// return next match in input
next:function () {
        if (this.done) {
            return this.EOF;
        }
        if (!this._input) {
            this.done = true;
        }

        var token,
            match,
            tempMatch,
            index;
        if (!this._more) {
            this.yytext = '';
            this.match = '';
        }
        var rules = this._currentRules();
        for (var i = 0; i < rules.length; i++) {
            tempMatch = this._input.match(this.rules[rules[i]]);
            if (tempMatch && (!match || tempMatch[0].length > match[0].length)) {
                match = tempMatch;
                index = i;
                if (this.options.backtrack_lexer) {
                    token = this.test_match(tempMatch, rules[i]);
                    if (token !== false) {
                        return token;
                    } else if (this._backtrack) {
                        match = false;
                        continue; // rule action called reject() implying a rule MISmatch.
                    } else {
                        // else: this is a lexer rule which consumes input without producing a token (e.g. whitespace)
                        return false;
                    }
                } else if (!this.options.flex) {
                    break;
                }
            }
        }
        if (match) {
            token = this.test_match(match, rules[index]);
            if (token !== false) {
                return token;
            }
            // else: this is a lexer rule which consumes input without producing a token (e.g. whitespace)
            return false;
        }
        if (this._input === "") {
            return this.EOF;
        } else {
            return this.parseError('Lexical error on line ' + (this.yylineno + 1) + '. Unrecognized text.\n' + this.showPosition(), {
                text: "",
                token: null,
                line: this.yylineno
            });
        }
    },

// return next match that has a token
lex:function lex() {
        var r = this.next();
        if (r) {
            return r;
        } else {
            return this.lex();
        }
    },

// activates a new lexer condition state (pushes the new lexer condition state onto the condition stack)
begin:function begin(condition) {
        this.conditionStack.push(condition);
    },

// pop the previously active lexer condition state off the condition stack
popState:function popState() {
        var n = this.conditionStack.length - 1;
        if (n > 0) {
            return this.conditionStack.pop();
        } else {
            return this.conditionStack[0];
        }
    },

// produce the lexer rule set which is active for the currently active lexer condition state
_currentRules:function _currentRules() {
        if (this.conditionStack.length && this.conditionStack[this.conditionStack.length - 1]) {
            return this.conditions[this.conditionStack[this.conditionStack.length - 1]].rules;
        } else {
            return this.conditions["INITIAL"].rules;
        }
    },

// return the currently active lexer condition state; when an index argument is provided it produces the N-th previous condition state, if available
topState:function topState(n) {
        n = this.conditionStack.length - 1 - Math.abs(n || 0);
        if (n >= 0) {
            return this.conditionStack[n];
        } else {
            return "INITIAL";
        }
    },

// alias for begin(condition)
pushState:function pushState(condition) {
        this.begin(condition);
    },

// return the number of states currently on the stack
stateStackSize:function stateStackSize() {
        return this.conditionStack.length;
    },
options: {},
performAction: function anonymous(yy,yy_,$avoiding_name_collisions,YY_START) {
    var debug = require('debug')('jison-lesslint: lexer');
    var s, s2, s3;
    var rv, rv2, e_offset, col, row, len, value;
    var match, match2;

    // console.log('lexer action: ', yy, yy_, this, yy_.yytext, YY_START, $avoiding_name_collisions);
    // var parser = yy.parser;
    debug(YY_START);

var YYSTATE=YY_START;
switch($avoiding_name_collisions) {
case 0:
    /* // 这个注释是为了把上面一行的 / 所带来的高亮影响给去掉 */
    yy_.yytext = yy_.yytext.replace(/^\n+/g, '');
    return 13;

break;
case 1:
    if (this.topState() !== 'sc') {
        this.begin('sc');
    }
    return 12;

break;
case 2:
    this.popState();

break;
case 3:
    /* // 这个注释是为了把上面一行的 / 所带来的高亮影响给去掉 */
    yy_.yytext = yy_.yytext.replace(/^\n+/g, '');
    return 16;

break;
case 4:
    if (this.topState() !== 'mc') {
        this.begin('mc');
    }
    return 14;

break;
case 5:
    this.popState();
    return 15;

break;
case 6:
    this.popState();

break;
case 7:
    /* // 这个注释是为了把上面一行的 / 所带来的高亮影响给去掉 */
    yy_.yytext = yy_.yytext.replace(/^\n+/g, '');
    return 45;

break;
case 8:
    this.begin('vari_start');
    return 46;

break;
case 9:
    this.popState();
    this.begin('vari');
    return 47;

break;
case 10:
    this.begin('vari_colon_start');
    return 49;

break;
case 11:
    this.popState();
    return 50;

break;
case 12:
    return 45;

break;
case 13:
    this.popState();
    return 52;

break;
case 14:
    /* // 这个注释是为了把上面一行的 / 所带来的高亮影响给去掉 */
    yy_.yytext = yy_.yytext.replace(/^\n+/g, '');
    return 24;

break;
case 15:
    this.begin('ch_start');
    return 17;

break;
case 16:
    this.popState();
    this.begin('ch');
    return 18;

break;
case 17:
    return 24;

break;
case 18:
    return 67;

break;
case 19:
    return 68;

break;
case 20:
    this.popState();
    return 22;

break;
case 21:
    /* // 这个注释是为了把上面一行的 / 所带来的高亮影响给去掉 */
    yy_.yytext = yy_.yytext.replace(/^\n+/g, '');
    return 29;

break;
case 22:
    this.begin('im_start');
    return 30;

break;
case 23:
    this.popState();
    this.begin('im');
    return 31;

break;
case 24:
    return 70;

break;
case 25:
    return 29;

break;
case 26:
    return 71;

break;
case 27:
    return 72;

break;
case 28:
    return 73;

break;
case 29:
    this.popState();
    return 37;

break;
case 30:
    /* // 这个注释是为了把上面一行的 / 所带来的高亮影响给去掉 */
    yy_.yytext = yy_.yytext.replace(/^\n+/g, '');
    this.begin('sel_start');
    return 57;

break;
case 31:
    this.begin('sel');
    return 58;

break;
case 32:
    return 74;

break;
case 33:
    this.begin('block');
    return 60;

break;
case 34:
    return 75;

break;
case 35:
    return 65;

break;
case 36:
    this.popState();
    return 62;

break;
case 37:
    /* // 这个注释是为了把上面一行的 / 所带来的高亮影响给去掉 */
    this.popState();
    return 4;

break;
case 38:
    return 69;

break;
case 39:
    /* // 这个注释是为了把上面一行的 / 所带来的高亮影响给去掉 */
    return 4;

break;
case 40:
    return 'N';

break;
case 41:
    this.popState();
    return 4;

break;
case 42:
    return 4;

break;
case 43:
    this.begin('s');

break;
}
},
rules: [/^(?:([ \t\s]+)(?=(\/\/[^\n]*)))/,/^(?:(\/\/[^\n]*))/,/^(?:(\n+))/,/^(?:([ \t\s]+)(?=(\/\*(?:[^*]|\*+[^\/*])*\n?)))/,/^(?:(\/\*(?:[^*]|\*+[^\/*])*\n?))/,/^(?:\*+\/[\s\n]?)/,/^(?:(\n+))/,/^(?:([ \t\s]+)(?=(@([\w-]+)\s*:+)))/,/^(?:@(?=(([\w-]+)\s*:+)))/,/^(?:([\w-]+))/,/^(?::+)/,/^(?:\s*(%|@?[_A-Za-z0-9-]|(("([^\n\r\f\\"])*")|('([^\n\r\f\\'])*'))|(#([A-Fa-f0-9]{6}|[A-Fa-f0-9]{3}))|[\(\)\+\-\*\/\s]*|,\s*)*)/,/^(?:([ \t\s]+))/,/^(?:(;+))/,/^(?:([ \t\s]+)(?=@charset))/,/^(?:@(?=charset))/,/^(?:charset\b)/,/^(?:([ \t\s]+))/,/^(?:(("([^\n\r\f\\"])*")|('([^\n\r\f\\'])*')))/,/^(?:([\w-]+))/,/^(?:(;+))/,/^(?:([ \t\s]+)(?=@import))/,/^(?:@(?=import))/,/^(?:import\b)/,/^(?:([ \t\s]+)\((\s*(|less|css|multiple|once|inline|reference)\s*(,\s*(|less|css|multiple|once|inline|reference)*\s*)*)\))/,/^(?:([ \t\s]+))/,/^(?:(("([^\n\r\f\\"])*")|('([^\n\r\f\\'])*')))/,/^(?:url\([^\)]+\))/,/^(?:[\w-\s\:\(\)]+)/,/^(?:(;+))/,/^(?:([ \t\s]+)*(?=((\*))))/,/^(?:((\*)))/,/^(?:([ \t\s]+))/,/^(?:(\{))/,/^(?:([ \t\s]+))/,/^(?:(;+))/,/^(?:(\}))/,/^(?:([ \t\s]+)(?=$))/,/^(?:([ \t\s]+))/,/^(?:(\n+)(?=$))/,/^(?:(\n+))/,/^(?:$)/,/^(?:$)/,/^(?:)/],
conditions: {"s":{"rules":[0,1,3,4,7,8,14,15,21,22,30,37,38,39,40,41],"inclusive":false},"sc":{"rules":[1,2,41],"inclusive":false},"mc":{"rules":[4,5,6,41],"inclusive":false},"ch_start":{"rules":[16],"inclusive":false},"ch":{"rules":[17,18,19,20],"inclusive":false},"im_start":{"rules":[23],"inclusive":false},"im":{"rules":[24,25,26,27,28,29],"inclusive":false},"vari_start":{"rules":[9],"inclusive":false},"vari":{"rules":[10,12,13],"inclusive":false},"vari_colon_start":{"rules":[11],"inclusive":false},"sel_start":{"rules":[31],"inclusive":false},"sel":{"rules":[32,33,41],"inclusive":false},"block":{"rules":[34,35,36],"inclusive":false},"INITIAL":{"rules":[42,43],"inclusive":true}}
});
return lexer;
})();
parser.lexer = lexer;
function Parser () {
  this.yy = {};
}
Parser.prototype = parser;parser.Parser = Parser;
return new Parser;
})();


if (typeof require !== 'undefined' && typeof exports !== 'undefined') {
exports.parser = parser;
exports.Parser = parser.Parser;
exports.parse = function () { return parser.parse.apply(parser, arguments); };
exports.main = function commonjsMain(args) {
    if (!args[1]) {
        console.log('Usage: '+args[0]+' FILE');
        process.exit(1);
    }
    var source = require('fs').readFileSync(require('path').normalize(args[1]), "utf8");
    return exports.parser.parse(source);
};
if (typeof module !== 'undefined' && require.main === module) {
  exports.main(process.argv.slice(1));
}
}